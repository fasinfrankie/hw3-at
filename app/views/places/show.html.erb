<h1><%= @place.name %></h1>

<h3>Posts</h3>
<ul>
    <% for my_posts in @place.posts %>
        <li>
         <%= my_posts.title %> --- Posted on: <%= my_posts.posted_on %></p>
            <p><%= my_posts.description %></p>
        </li>
    <% end %>
</ul>

<a href="/posts/new?place_id=<%= @place.id %>">Add Post</a>

<a href="/places">Back to all places</a>

